apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: api
  ports:
    - port: {{ .Values.service.port }}   # 80 by default
      targetPort: 8080                   # forwards to container port
